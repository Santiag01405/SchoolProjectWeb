@model SchoolProjectWeb.Models.ClassroomViewModel

@{
    ViewData["Title"] = "Crear Salón";
}

<h1 class="main-title">Crear <span class="brand-name">Salón</span></h1>
<p class="subtitle">Llena los datos para crear un nuevo salón.</p>
<hr />

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="form-box">
            <form asp-action="CreateClassroom" method="post">
                <div class="form-group">
                    <label asp-for="Name" class="control-label"></label>
                    <input asp-for="Name" class="form-control" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                </div>
                <div class="form-group mt-3">
                    <label asp-for="Description" class="control-label"></label>
                    <textarea asp-for="Description" class="form-control" rows="3"></textarea>
                    <span asp-validation-for="Description" class="text-danger"></span>
                </div>
                <div class="form-group mt-4 text-center">
                    <input type="submit" value="Crear Salón" class="btn btn-primary-custom" />
                    <a asp-action="ListClassrooms" class="btn btn-secondary-custom ms-2">Volver a la lista</a>
                </div>
            </form>
        </div>
    </div>
</div>